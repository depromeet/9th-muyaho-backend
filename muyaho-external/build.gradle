bootJar { enabled = false }
jar { enabled = true }

dependencies {
    implementation project(':muyaho-common')

    // spring webflux
    implementation 'org.springframework.boot:spring-boot-starter-webflux'
}

server:
  port: 9090
  servlet:
    encoding:
      charset: UTF-8
spring:
  profiles:
    active: local
    include:
      - domain
      - swagger
      - stock
      - rds
      - oauth
  session:
    store-type: jdbc
  codec:
    log-request-details: true
logging:
  file:
    path: logs/api/

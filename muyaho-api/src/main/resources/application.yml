server:
  port: 9090
  servlet:
    encoding:
      charset: UTF-8
spring:
  profiles:
    active: local
    include:
      - domain
      - swagger
      - market
      - rds
      - oauth
      - currency
  session:
    store-type: jdbc
